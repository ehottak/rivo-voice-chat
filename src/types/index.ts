export type {
  ParticipantInfo,
  JoinRoomData,
  JoinRoomResult,
  VoiceOffer,
  VoiceAnswer,
  VoiceIceCandidate,
  ServerToClientEvents,
  ClientToServerEvents,
  InterServerEvents,
  SocketData,
} from './socket-events';

export type { RoomInfo, Participant } from './room';
